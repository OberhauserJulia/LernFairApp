export interface PopUpCompleteFileProps {
    visible: boolean;
    hideModal: () => void;
    file_id : string;  
    filename : string; 
  
  }

  export interface PopUpCompleteFilePropsArchive {
    visible: boolean;
    hideModal: () => void;
    file_id ? : string;  
    filename ? : string; 
  
  }